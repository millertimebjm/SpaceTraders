@using SpaceTraders.Models
@using SpaceTraders.Mvc.Services
@model ShipViewModel
@{
    var ship = await Model.ShipTask;
    ViewData["Title"] = $"Sh {ship.Symbol}";
}

@await Html.PartialAsync("_Ship", Model)